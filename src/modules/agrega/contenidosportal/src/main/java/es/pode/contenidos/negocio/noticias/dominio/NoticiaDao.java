// license-header java merge-point
//
// Attention: Generated code! Do not modify by hand!
// Generated by: SpringDao.vsl in andromda-spring-cartridge.
//
package es.pode.contenidos.negocio.noticias.dominio;

/**
 * @see es.pode.contenidos.negocio.noticias.dominio.Noticia
 */
public interface NoticiaDao
{
    /**
     * This constant is used as a transformation flag; entities can be converted automatically into value objects
     * or other types, different methods in a class implementing this interface support this feature: look for
     * an <code>int</code> parameter called <code>transform</code>.
     * <p/>
     * This specific flag denotes no transformation will occur.
     */
    public final static int TRANSFORM_NONE = 0;

    /**
     * This constant is used as a transformation flag; entities can be converted automatically into value objects
     * or other types, different methods in a class implementing this interface support this feature: look for
     * an <code>int</code> parameter called <code>transform</code>.
     * <p/>
     * This specific flag denotes entities must be transformed into objects of type
     * {@link es.pode.contenidos.negocio.noticias.servicio.NoticiaVO}.
     */
    public final static int TRANSFORM_NOTICIAVO = 1;

    /**
     * Converts this DAO's entity to an object of type {@link es.pode.contenidos.negocio.noticias.servicio.NoticiaVO}.
     */
    public es.pode.contenidos.negocio.noticias.servicio.NoticiaVO toNoticiaVO(es.pode.contenidos.negocio.noticias.dominio.Noticia entity);

    /**
     * Converts this DAO's entity to a Collection of instances of type {@link es.pode.contenidos.negocio.noticias.servicio.NoticiaVO}.
     */
    public void toNoticiaVOCollection(java.util.Collection entities);

    public es.pode.contenidos.negocio.noticias.dominio.Noticia fromNoticiaVO(es.pode.contenidos.negocio.noticias.servicio.NoticiaVO vo);
    
    public void fromNoticiaVO(es.pode.contenidos.negocio.noticias.servicio.NoticiaVO vo, es.pode.contenidos.negocio.noticias.dominio.Noticia entity);    


    /**
     * Loads an instance of es.pode.contenidos.negocio.noticias.dominio.Noticia from the persistent store.
     */
    public es.pode.contenidos.negocio.noticias.dominio.Noticia load(java.lang.Long id);

    /**
     * <p>
     *   Does the same thing as {@link #load(java.lang.Long)} with an
     *   additional flag called <code>transform</code>.  If this flag is set to <code>TRANSFORM_NONE</code> then
     *   the returned entity will <strong>NOT</strong> be transformed.  If this flag is any of the other constants
     *   defined in this class then the result <strong>WILL BE</strong> passed through an operation which can
     *   optionally transform the entity (into a value object for example).  By default, transformation does
     *   not occur.
     * </p>
     *
     * @param id the identifier of the entity to load.
     * @return either the entity or the object transformed from the entity.
     */
    public Object load(int transform, java.lang.Long id);
    
    /**
     * Loads all entities of type {@link es.pode.contenidos.negocio.noticias.dominio.Noticia}. 
     * 
     * @return the loaded entities.
     */
    public java.util.Collection loadAll();
    
    /** 
     * <p>
     *   Does the same thing as {@link #loadAll()} with an
     *   additional flag called <code>transform</code>.  If this flag is set to <code>TRANSFORM_NONE</code> then
     *   the returned entity will <strong>NOT</strong> be transformed.  If this flag is any of the other constants
     *   defined here then the result <strong>WILL BE</strong> passed through an operation which can optionally
     *   transform the entity (into a value object for example).  By default, transformation does
     *   not occur.
     * </p>
     * 
     * @param transform the flag indicating what transformation to use.
     * @return the loaded entities.
     */
    public java.util.Collection loadAll(final int transform);

    /**
     * Creates an instance of es.pode.contenidos.negocio.noticias.dominio.Noticia and adds it to the persistent store.
     */
    public es.pode.contenidos.negocio.noticias.dominio.Noticia create(es.pode.contenidos.negocio.noticias.dominio.Noticia noticia);

    /**
     * <p>
     *   Does the same thing as {@link #create(es.pode.contenidos.negocio.noticias.dominio.Noticia)} with an
     *   additional flag called <code>transform</code>.  If this flag is set to <code>TRANSFORM_NONE</code> then
     *   the returned entity will <strong>NOT</strong> be transformed.  If this flag is any of the other constants
     *   defined here then the result <strong>WILL BE</strong> passed through an operation which can optionally
     *   transform the entity (into a value object for example).  By default, transformation does
     *   not occur.
     * </p>
     */
    public Object create(int transform, es.pode.contenidos.negocio.noticias.dominio.Noticia noticia);

    /**
     * Creates a new instance of es.pode.contenidos.negocio.noticias.dominio.Noticia and adds
     * from the passed in <code>entities</code> collection
     *
     * @param entities the collection of es.pode.contenidos.negocio.noticias.dominio.Noticia
     *        instances to create.
     *
     * @return the created instances.
     */
    public java.util.Collection create(java.util.Collection entities);

    /**
     * <p>
     *   Does the same thing as {@link #create(es.pode.contenidos.negocio.noticias.dominio.Noticia)} with an
     *   additional flag called <code>transform</code>.  If this flag is set to <code>TRANSFORM_NONE</code> then
     *   the returned entity will <strong>NOT</strong> be transformed.  If this flag is any of the other constants
     *   defined here then the result <strong>WILL BE</strong> passed through an operation which can optionally
     *   transform the entities (into value objects for example).  By default, transformation does
     *   not occur.
     * </p>
     */
    public java.util.Collection create(int transform, java.util.Collection entities);

    /**
     * <p>
     *   Creates a new <code>es.pode.contenidos.negocio.noticias.dominio.Noticia</code>
     *   instance from <strong>all</strong> attributes and adds it to
     *   the persistent store.
     * </p>
     */
    public es.pode.contenidos.negocio.noticias.dominio.Noticia create(
        java.util.Date fechaPublicacion,
        java.lang.String URLImagen,
        java.lang.Integer alineamientoImg,
        java.lang.Boolean activa,
        java.lang.String autor,
        java.util.Date fechaBaja,
        java.util.Date fechaModificacion);

    /**
     * <p>
     *   Does the same thing as {@link #create(java.util.Date, java.lang.String, java.lang.Integer, java.lang.Boolean, java.lang.String, java.util.Date, java.util.Date)} with an
     *   additional flag called <code>transform</code>.  If this flag is set to <code>TRANSFORM_NONE</code> then
     *   the returned entity will <strong>NOT</strong> be transformed.  If this flag is any of the other constants
     *   defined here then the result <strong>WILL BE</strong> passed through an operation which can optionally
     *   transform the entity (into a value object for example).  By default, transformation does
     *   not occur.
     * </p>
     */
    public Object create(
        int transform,
        java.util.Date fechaPublicacion,
        java.lang.String URLImagen,
        java.lang.Integer alineamientoImg,
        java.lang.Boolean activa,
        java.lang.String autor,
        java.util.Date fechaBaja,
        java.util.Date fechaModificacion);

    /**
     * <p>
     *  Creates a new <code>es.pode.contenidos.negocio.noticias.dominio.Noticia</code>
     *  instance from only <strong>required</strong> properties (attributes
     *  and association ends) and adds it to the persistent store.
     * </p>
     */
    public es.pode.contenidos.negocio.noticias.dominio.Noticia create(
        es.pode.contenidos.negocio.noticias.dominio.CategoriaNoticia categoria,
        java.util.Collection descripcionNoticia);

    /**
     * <p>
     *   Does the same thing as {@link #create()} with an
     *   additional flag called <code>transform</code>.  If this flag is set to <code>TRANSFORM_NONE</code> then
     *   the returned entity will <strong>NOT</strong be transformed.  If this flag is any of the other constants
     *   defined here then the result <strong>WILL BE</strong> passed through an operation which can optionally
     *   transform the entity (into a value object for example).  By default, transformation does
     *   not occur.
     * </p>
     */
    public Object create(
        int transform,
        es.pode.contenidos.negocio.noticias.dominio.CategoriaNoticia categoria,
        java.util.Collection descripcionNoticia);

    /**
     * Updates the <code>noticia</code> instance in the persistent store.
     */
    public void update(es.pode.contenidos.negocio.noticias.dominio.Noticia noticia);

    /**
     * Updates all instances in the <code>entities</code> collection in the persistent store.
     */
    public void update(java.util.Collection entities);

    /**
     * Removes the instance of es.pode.contenidos.negocio.noticias.dominio.Noticia from the persistent store.
     */
    public void remove(es.pode.contenidos.negocio.noticias.dominio.Noticia noticia);

    /**
     * Removes the instance of es.pode.contenidos.negocio.noticias.dominio.Noticia having the given
     * <code>identifier</code> from the persistent store.
     */
    public void remove(java.lang.Long id);

    /**
     * Removes all entities in the given <code>entities<code> collection.
     */
    public void remove(java.util.Collection entities);

    /**
     * <p>
     * Obtiene un listado de las noticias activas redactadas en el
     * idioma deseado. Ordena el listado por fecha de publicacion.
     * </p>
     */
    public java.util.List obtenerNoticiasActivasPorIdioma(java.lang.String autor);

    /**
     * <p>
     *   Does the same thing as {@link #obtenerNoticiasActivasPorIdioma(java.lang.String)} with an
     *   additional argument called <code>queryString</code>.  This <code>queryString</code>
     *   argument allows you to override the query string defined in {@link #obtenerNoticiasActivasPorIdioma(java.lang.String)}.
     * </p>
     */
    public java.util.List obtenerNoticiasActivasPorIdioma(String queryString, java.lang.String autor);

    /**
     * <p>
     *   Does the same thing as {@link #obtenerNoticiasActivasPorIdioma(java.lang.String)} with an
     *   additional flag called <code>transform</code>.  If this flag is set to <code>TRANSFORM_NONE</code> then
     *   finder results will <strong>NOT</strong> be transformed during retrieval.
     *   If this flag is any of the other constants defined here
     *   then finder results <strong>WILL BE</strong> passed through an operation which can optionally
     *   transform the entities (into value objects for example).  By default, transformation does
     *   not occur.
     * </p>
     */
    public java.util.List obtenerNoticiasActivasPorIdioma(int transform, java.lang.String autor);

    /**
     * <p>
     *   Does the same thing as {@link #obtenerNoticiasActivasPorIdioma(boolean, java.lang.String)} with an
     *   additional argument called <code>queryString</code>.  This <code>queryString</code>
     *   argument allows you to override the query string defined in {@link #obtenerNoticiasActivasPorIdioma(int, java.lang.String autor)}.
     * </p>
     */
    public java.util.List obtenerNoticiasActivasPorIdioma(int transform, String queryString, java.lang.String autor);

    /**
     * <p>
     * Obtiene un listado de las noticias activas pertenecientes a una
     * categoria y redactadas en el idioma deseado. Ordena el listado
     * por fecha de publicacion.
     * </p>
     */
    public java.util.Collection obtenerNoticiasActivasPorIdiomayCategoria(java.lang.String autor, java.lang.Long id);

    /**
     * <p>
     *   Does the same thing as {@link #obtenerNoticiasActivasPorIdiomayCategoria(java.lang.String, java.lang.Long)} with an
     *   additional argument called <code>queryString</code>.  This <code>queryString</code>
     *   argument allows you to override the query string defined in {@link #obtenerNoticiasActivasPorIdiomayCategoria(java.lang.String, java.lang.Long)}.
     * </p>
     */
    public java.util.Collection obtenerNoticiasActivasPorIdiomayCategoria(String queryString, java.lang.String autor, java.lang.Long id);

    /**
     * <p>
     *   Does the same thing as {@link #obtenerNoticiasActivasPorIdiomayCategoria(java.lang.String, java.lang.Long)} with an
     *   additional flag called <code>transform</code>.  If this flag is set to <code>TRANSFORM_NONE</code> then
     *   finder results will <strong>NOT</strong> be transformed during retrieval.
     *   If this flag is any of the other constants defined here
     *   then finder results <strong>WILL BE</strong> passed through an operation which can optionally
     *   transform the entities (into value objects for example).  By default, transformation does
     *   not occur.
     * </p>
     */
    public java.util.Collection obtenerNoticiasActivasPorIdiomayCategoria(int transform, java.lang.String autor, java.lang.Long id);

    /**
     * <p>
     *   Does the same thing as {@link #obtenerNoticiasActivasPorIdiomayCategoria(boolean, java.lang.String, java.lang.Long)} with an
     *   additional argument called <code>queryString</code>.  This <code>queryString</code>
     *   argument allows you to override the query string defined in {@link #obtenerNoticiasActivasPorIdiomayCategoria(int, java.lang.String autor, java.lang.Long id)}.
     * </p>
     */
    public java.util.Collection obtenerNoticiasActivasPorIdiomayCategoria(int transform, String queryString, java.lang.String autor, java.lang.Long id);

    /**
     * <p>
     * Obtiene un listado de todas las noticias ordenado por fecha de
     * publicación.
     * </p>
     */
    public java.util.List obtenerNoticias();

    /**
     * <p>
     *   Does the same thing as {@link #obtenerNoticias()} with an
     *   additional argument called <code>queryString</code>.  This <code>queryString</code>
     *   argument allows you to override the query string defined in {@link #obtenerNoticias()}.
     * </p>
     */
    public java.util.List obtenerNoticias(String queryString);

    /**
     * <p>
     *   Does the same thing as {@link #obtenerNoticias()} with an
     *   additional flag called <code>transform</code>.  If this flag is set to <code>TRANSFORM_NONE</code> then
     *   finder results will <strong>NOT</strong> be transformed during retrieval.
     *   If this flag is any of the other constants defined here
     *   then finder results <strong>WILL BE</strong> passed through an operation which can optionally
     *   transform the entities (into value objects for example).  By default, transformation does
     *   not occur.
     * </p>
     */
    public java.util.List obtenerNoticias(int transform);

    /**
     * <p>
     *   Does the same thing as {@link #obtenerNoticias(boolean)} with an
     *   additional argument called <code>queryString</code>.  This <code>queryString</code>
     *   argument allows you to override the query string defined in {@link #obtenerNoticias(int)}.
     * </p>
     */
    public java.util.List obtenerNoticias(int transform, String queryString);

    /**
     * 
     */
    public java.util.List obtenerNoticiasActivas(es.pode.contenidos.negocio.noticias.dominio.NoticiasActivasFechaCriteria criterio);

    /**
     * <p>
     *   Does the same thing as {@link #obtenerNoticiasActivas(es.pode.contenidos.negocio.noticias.dominio.NoticiasActivasFechaCriteria)} with an
     *   additional flag called <code>transform</code>.  If this flag is set to <code>TRANSFORM_NONE</code> then
     *   finder results will <strong>NOT</strong> be transformed during retrieval.
     *   If this flag is any of the other constants defined here
     *   then finder results <strong>WILL BE</strong> passed through an operation which can optionally
     *   transform the entities (into value objects for example).  By default, transformation does
     *   not occur.
     * </p>
     */
    public java.util.List obtenerNoticiasActivas(final int transform, final es.pode.contenidos.negocio.noticias.dominio.NoticiasActivasFechaCriteria criterio);

    /**
     * <p>
     * Consulta las noticias disponibles en el idioma deseado. El
     * idioma se identifica mediante el codigo ISO-639 correspondiente.
     * </p>
     */
    public java.util.List obtenerNoticiasPorIdioma(java.lang.String autor);

    /**
     * <p>
     *   Does the same thing as {@link #obtenerNoticiasPorIdioma(java.lang.String)} with an
     *   additional argument called <code>queryString</code>.  This <code>queryString</code>
     *   argument allows you to override the query string defined in {@link #obtenerNoticiasPorIdioma(java.lang.String)}.
     * </p>
     */
    public java.util.List obtenerNoticiasPorIdioma(String queryString, java.lang.String autor);

    /**
     * <p>
     *   Does the same thing as {@link #obtenerNoticiasPorIdioma(java.lang.String)} with an
     *   additional flag called <code>transform</code>.  If this flag is set to <code>TRANSFORM_NONE</code> then
     *   finder results will <strong>NOT</strong> be transformed during retrieval.
     *   If this flag is any of the other constants defined here
     *   then finder results <strong>WILL BE</strong> passed through an operation which can optionally
     *   transform the entities (into value objects for example).  By default, transformation does
     *   not occur.
     * </p>
     */
    public java.util.List obtenerNoticiasPorIdioma(int transform, java.lang.String autor);

    /**
     * <p>
     *   Does the same thing as {@link #obtenerNoticiasPorIdioma(boolean, java.lang.String)} with an
     *   additional argument called <code>queryString</code>.  This <code>queryString</code>
     *   argument allows you to override the query string defined in {@link #obtenerNoticiasPorIdioma(int, java.lang.String autor)}.
     * </p>
     */
    public java.util.List obtenerNoticiasPorIdioma(int transform, String queryString, java.lang.String autor);

    /**
     * <p>
     * Obtiene los idiomas en los que está traducida la noticia con
     * identificador id.
     * </p>
     */
    public java.util.List obtenerIdiomasFromIdNoticia(java.lang.Long id);

    /**
     * <p>
     *   Does the same thing as {@link #obtenerIdiomasFromIdNoticia(java.lang.Long)} with an
     *   additional argument called <code>queryString</code>.  This <code>queryString</code>
     *   argument allows you to override the query string defined in {@link #obtenerIdiomasFromIdNoticia(java.lang.Long)}.
     * </p>
     */
    public java.util.List obtenerIdiomasFromIdNoticia(String queryString, java.lang.Long id);

    /**
     * <p>
     *   Does the same thing as {@link #obtenerIdiomasFromIdNoticia(java.lang.Long)} with an
     *   additional flag called <code>transform</code>.  If this flag is set to <code>TRANSFORM_NONE</code> then
     *   finder results will <strong>NOT</strong> be transformed during retrieval.
     *   If this flag is any of the other constants defined here
     *   then finder results <strong>WILL BE</strong> passed through an operation which can optionally
     *   transform the entities (into value objects for example).  By default, transformation does
     *   not occur.
     * </p>
     */
    public java.util.List obtenerIdiomasFromIdNoticia(int transform, java.lang.Long id);

    /**
     * <p>
     *   Does the same thing as {@link #obtenerIdiomasFromIdNoticia(boolean, java.lang.Long)} with an
     *   additional argument called <code>queryString</code>.  This <code>queryString</code>
     *   argument allows you to override the query string defined in {@link #obtenerIdiomasFromIdNoticia(int, java.lang.Long id)}.
     * </p>
     */
    public java.util.List obtenerIdiomasFromIdNoticia(int transform, String queryString, java.lang.Long id);

    /**
     * <p>
     * Obtiene a partir del identificador de la categoría todas las
     * noticias que están relacionadas con ella.
     * </p>
     */
    public java.util.List obtenerNoticiasByIdCategoria(java.lang.Long id);

    /**
     * <p>
     *   Does the same thing as {@link #obtenerNoticiasByIdCategoria(java.lang.Long)} with an
     *   additional argument called <code>queryString</code>.  This <code>queryString</code>
     *   argument allows you to override the query string defined in {@link #obtenerNoticiasByIdCategoria(java.lang.Long)}.
     * </p>
     */
    public java.util.List obtenerNoticiasByIdCategoria(String queryString, java.lang.Long id);

    /**
     * <p>
     *   Does the same thing as {@link #obtenerNoticiasByIdCategoria(java.lang.Long)} with an
     *   additional flag called <code>transform</code>.  If this flag is set to <code>TRANSFORM_NONE</code> then
     *   finder results will <strong>NOT</strong> be transformed during retrieval.
     *   If this flag is any of the other constants defined here
     *   then finder results <strong>WILL BE</strong> passed through an operation which can optionally
     *   transform the entities (into value objects for example).  By default, transformation does
     *   not occur.
     * </p>
     */
    public java.util.List obtenerNoticiasByIdCategoria(int transform, java.lang.Long id);

    /**
     * <p>
     *   Does the same thing as {@link #obtenerNoticiasByIdCategoria(boolean, java.lang.Long)} with an
     *   additional argument called <code>queryString</code>.  This <code>queryString</code>
     *   argument allows you to override the query string defined in {@link #obtenerNoticiasByIdCategoria(int, java.lang.Long id)}.
     * </p>
     */
    public java.util.List obtenerNoticiasByIdCategoria(int transform, String queryString, java.lang.Long id);

}
