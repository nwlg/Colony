// license-header java merge-point
//
// Attention: Generated code! Do not modify by hand!
// Generated by: SpringDao.vsl in andromda-spring-cartridge.
//
package es.pode.contenidos.negocio.faqs.dominio;

/**
 * @see es.pode.contenidos.negocio.faqs.dominio.CategoriaFaq
 */
public interface CategoriaFaqDao
{
    /**
     * This constant is used as a transformation flag; entities can be converted automatically into value objects
     * or other types, different methods in a class implementing this interface support this feature: look for
     * an <code>int</code> parameter called <code>transform</code>.
     * <p/>
     * This specific flag denotes no transformation will occur.
     */
    public final static int TRANSFORM_NONE = 0;

    /**
     * This constant is used as a transformation flag; entities can be converted automatically into value objects
     * or other types, different methods in a class implementing this interface support this feature: look for
     * an <code>int</code> parameter called <code>transform</code>.
     * <p/>
     * This specific flag denotes entities must be transformed into objects of type
     * {@link es.pode.contenidos.negocio.faqs.servicio.CategoriaFaqVO}.
     */
    public final static int TRANSFORM_CATEGORIAFAQVO = 1;

    /**
     * Converts this DAO's entity to an object of type {@link es.pode.contenidos.negocio.faqs.servicio.CategoriaFaqVO}.
     */
    public es.pode.contenidos.negocio.faqs.servicio.CategoriaFaqVO toCategoriaFaqVO(es.pode.contenidos.negocio.faqs.dominio.CategoriaFaq entity);

    /**
     * Converts this DAO's entity to a Collection of instances of type {@link es.pode.contenidos.negocio.faqs.servicio.CategoriaFaqVO}.
     */
    public void toCategoriaFaqVOCollection(java.util.Collection entities);

    public es.pode.contenidos.negocio.faqs.dominio.CategoriaFaq fromCategoriaFaqVO(es.pode.contenidos.negocio.faqs.servicio.CategoriaFaqVO vo);
    
    public void fromCategoriaFaqVO(es.pode.contenidos.negocio.faqs.servicio.CategoriaFaqVO vo, es.pode.contenidos.negocio.faqs.dominio.CategoriaFaq entity);    


    /**
     * Loads an instance of es.pode.contenidos.negocio.faqs.dominio.CategoriaFaq from the persistent store.
     */
    public es.pode.contenidos.negocio.faqs.dominio.CategoriaFaq load(java.lang.Long id);

    /**
     * <p>
     *   Does the same thing as {@link #load(java.lang.Long)} with an
     *   additional flag called <code>transform</code>.  If this flag is set to <code>TRANSFORM_NONE</code> then
     *   the returned entity will <strong>NOT</strong> be transformed.  If this flag is any of the other constants
     *   defined in this class then the result <strong>WILL BE</strong> passed through an operation which can
     *   optionally transform the entity (into a value object for example).  By default, transformation does
     *   not occur.
     * </p>
     *
     * @param id the identifier of the entity to load.
     * @return either the entity or the object transformed from the entity.
     */
    public Object load(int transform, java.lang.Long id);
    
    /**
     * Loads all entities of type {@link es.pode.contenidos.negocio.faqs.dominio.CategoriaFaq}. 
     * 
     * @return the loaded entities.
     */
    public java.util.Collection loadAll();
    
    /** 
     * <p>
     *   Does the same thing as {@link #loadAll()} with an
     *   additional flag called <code>transform</code>.  If this flag is set to <code>TRANSFORM_NONE</code> then
     *   the returned entity will <strong>NOT</strong> be transformed.  If this flag is any of the other constants
     *   defined here then the result <strong>WILL BE</strong> passed through an operation which can optionally
     *   transform the entity (into a value object for example).  By default, transformation does
     *   not occur.
     * </p>
     * 
     * @param transform the flag indicating what transformation to use.
     * @return the loaded entities.
     */
    public java.util.Collection loadAll(final int transform);

    /**
     * Creates an instance of es.pode.contenidos.negocio.faqs.dominio.CategoriaFaq and adds it to the persistent store.
     */
    public es.pode.contenidos.negocio.faqs.dominio.CategoriaFaq create(es.pode.contenidos.negocio.faqs.dominio.CategoriaFaq categoriaFaq);

    /**
     * <p>
     *   Does the same thing as {@link #create(es.pode.contenidos.negocio.faqs.dominio.CategoriaFaq)} with an
     *   additional flag called <code>transform</code>.  If this flag is set to <code>TRANSFORM_NONE</code> then
     *   the returned entity will <strong>NOT</strong> be transformed.  If this flag is any of the other constants
     *   defined here then the result <strong>WILL BE</strong> passed through an operation which can optionally
     *   transform the entity (into a value object for example).  By default, transformation does
     *   not occur.
     * </p>
     */
    public Object create(int transform, es.pode.contenidos.negocio.faqs.dominio.CategoriaFaq categoriaFaq);

    /**
     * Creates a new instance of es.pode.contenidos.negocio.faqs.dominio.CategoriaFaq and adds
     * from the passed in <code>entities</code> collection
     *
     * @param entities the collection of es.pode.contenidos.negocio.faqs.dominio.CategoriaFaq
     *        instances to create.
     *
     * @return the created instances.
     */
    public java.util.Collection create(java.util.Collection entities);

    /**
     * <p>
     *   Does the same thing as {@link #create(es.pode.contenidos.negocio.faqs.dominio.CategoriaFaq)} with an
     *   additional flag called <code>transform</code>.  If this flag is set to <code>TRANSFORM_NONE</code> then
     *   the returned entity will <strong>NOT</strong> be transformed.  If this flag is any of the other constants
     *   defined here then the result <strong>WILL BE</strong> passed through an operation which can optionally
     *   transform the entities (into value objects for example).  By default, transformation does
     *   not occur.
     * </p>
     */
    public java.util.Collection create(int transform, java.util.Collection entities);

    /**
     * <p>
     *   Creates a new <code>es.pode.contenidos.negocio.faqs.dominio.CategoriaFaq</code>
     *   instance from <strong>all</strong> attributes and adds it to
     *   the persistent store.
     * </p>
     */
    public es.pode.contenidos.negocio.faqs.dominio.CategoriaFaq create(
        java.lang.String nombre,
        java.util.Date fechaBaja,
        java.util.Date fechaModificacion,
        java.util.Date fechaAlta);

    /**
     * <p>
     *   Does the same thing as {@link #create(java.lang.String, java.util.Date, java.util.Date, java.util.Date)} with an
     *   additional flag called <code>transform</code>.  If this flag is set to <code>TRANSFORM_NONE</code> then
     *   the returned entity will <strong>NOT</strong> be transformed.  If this flag is any of the other constants
     *   defined here then the result <strong>WILL BE</strong> passed through an operation which can optionally
     *   transform the entity (into a value object for example).  By default, transformation does
     *   not occur.
     * </p>
     */
    public Object create(
        int transform,
        java.lang.String nombre,
        java.util.Date fechaBaja,
        java.util.Date fechaModificacion,
        java.util.Date fechaAlta);

    /**
     * <p>
     *  Creates a new <code>es.pode.contenidos.negocio.faqs.dominio.CategoriaFaq</code>
     *  instance from only <strong>required</strong> properties (attributes
     *  and association ends) and adds it to the persistent store.
     * </p>
     */
    public es.pode.contenidos.negocio.faqs.dominio.CategoriaFaq create(
        java.util.Collection categoriaIdioma,
        java.lang.String nombre);

    /**
     * <p>
     *   Does the same thing as {@link #create(java.lang.String)} with an
     *   additional flag called <code>transform</code>.  If this flag is set to <code>TRANSFORM_NONE</code> then
     *   the returned entity will <strong>NOT</strong be transformed.  If this flag is any of the other constants
     *   defined here then the result <strong>WILL BE</strong> passed through an operation which can optionally
     *   transform the entity (into a value object for example).  By default, transformation does
     *   not occur.
     * </p>
     */
    public Object create(
        int transform,
        java.util.Collection categoriaIdioma,
        java.lang.String nombre);

    /**
     * Updates the <code>categoriaFaq</code> instance in the persistent store.
     */
    public void update(es.pode.contenidos.negocio.faqs.dominio.CategoriaFaq categoriaFaq);

    /**
     * Updates all instances in the <code>entities</code> collection in the persistent store.
     */
    public void update(java.util.Collection entities);

    /**
     * Removes the instance of es.pode.contenidos.negocio.faqs.dominio.CategoriaFaq from the persistent store.
     */
    public void remove(es.pode.contenidos.negocio.faqs.dominio.CategoriaFaq categoriaFaq);

    /**
     * Removes the instance of es.pode.contenidos.negocio.faqs.dominio.CategoriaFaq having the given
     * <code>identifier</code> from the persistent store.
     */
    public void remove(java.lang.Long id);

    /**
     * Removes all entities in the given <code>entities<code> collection.
     */
    public void remove(java.util.Collection entities);

    /**
     * <p>
     * Obtiene un listado de las categorias de faq existentes.
     * </p>
     */
    public java.util.List obtenCategoriasFaqs(java.lang.String nombre);

    /**
     * <p>
     *   Does the same thing as {@link #obtenCategoriasFaqs(java.lang.String)} with an
     *   additional argument called <code>queryString</code>.  This <code>queryString</code>
     *   argument allows you to override the query string defined in {@link #obtenCategoriasFaqs(java.lang.String)}.
     * </p>
     */
    public java.util.List obtenCategoriasFaqs(String queryString, java.lang.String nombre);

    /**
     * <p>
     *   Does the same thing as {@link #obtenCategoriasFaqs(java.lang.String)} with an
     *   additional flag called <code>transform</code>.  If this flag is set to <code>TRANSFORM_NONE</code> then
     *   finder results will <strong>NOT</strong> be transformed during retrieval.
     *   If this flag is any of the other constants defined here
     *   then finder results <strong>WILL BE</strong> passed through an operation which can optionally
     *   transform the entities (into value objects for example).  By default, transformation does
     *   not occur.
     * </p>
     */
    public java.util.List obtenCategoriasFaqs(int transform, java.lang.String nombre);

    /**
     * <p>
     *   Does the same thing as {@link #obtenCategoriasFaqs(boolean, java.lang.String)} with an
     *   additional argument called <code>queryString</code>.  This <code>queryString</code>
     *   argument allows you to override the query string defined in {@link #obtenCategoriasFaqs(int, java.lang.String nombre)}.
     * </p>
     */
    public java.util.List obtenCategoriasFaqs(int transform, String queryString, java.lang.String nombre);

    /**
     * 
     */
    public es.pode.contenidos.negocio.faqs.servicio.CategoriaFaqVO obtenerCategoria(java.lang.Long id);

    /**
     * <p>
     *   Does the same thing as {@link #obtenerCategoria(java.lang.Long)} with an
     *   additional argument called <code>queryString</code>.  This <code>queryString</code>
     *   argument allows you to override the query string defined in {@link #obtenerCategoria(java.lang.Long)}.
     * </p>
     */
    public es.pode.contenidos.negocio.faqs.servicio.CategoriaFaqVO obtenerCategoria(String queryString, java.lang.Long id);

    /**
     * <p>
     *   Does the same thing as {@link #obtenerCategoria(java.lang.Long)} with an
     *   additional flag called <code>transform</code>.  If this flag is set to <code>TRANSFORM_NONE</code> then
     *   finder results will <strong>NOT</strong> be transformed during retrieval.
     *   If this flag is any of the other constants defined here
     *   then finder results <strong>WILL BE</strong> passed through an operation which can optionally
     *   transform the entities (into value objects for example).  By default, transformation does
     *   not occur.
     * </p>
     */
    public Object obtenerCategoria(int transform, java.lang.Long id);

    /**
     * <p>
     *   Does the same thing as {@link #obtenerCategoria(boolean, java.lang.Long)} with an
     *   additional argument called <code>queryString</code>.  This <code>queryString</code>
     *   argument allows you to override the query string defined in {@link #obtenerCategoria(int, java.lang.Long id)}.
     * </p>
     */
    public Object obtenerCategoria(int transform, String queryString, java.lang.Long id);

    /**
     * 
     */
    public java.util.Collection obtenerCategorias();

    /**
     * <p>
     *   Does the same thing as {@link #obtenerCategorias()} with an
     *   additional argument called <code>queryString</code>.  This <code>queryString</code>
     *   argument allows you to override the query string defined in {@link #obtenerCategorias()}.
     * </p>
     */
    public java.util.Collection obtenerCategorias(String queryString);

    /**
     * <p>
     *   Does the same thing as {@link #obtenerCategorias()} with an
     *   additional flag called <code>transform</code>.  If this flag is set to <code>TRANSFORM_NONE</code> then
     *   finder results will <strong>NOT</strong> be transformed during retrieval.
     *   If this flag is any of the other constants defined here
     *   then finder results <strong>WILL BE</strong> passed through an operation which can optionally
     *   transform the entities (into value objects for example).  By default, transformation does
     *   not occur.
     * </p>
     */
    public java.util.Collection obtenerCategorias(int transform);

    /**
     * <p>
     *   Does the same thing as {@link #obtenerCategorias(boolean)} with an
     *   additional argument called <code>queryString</code>.  This <code>queryString</code>
     *   argument allows you to override the query string defined in {@link #obtenerCategorias(int)}.
     * </p>
     */
    public java.util.Collection obtenerCategorias(int transform, String queryString);

    /**
     * <p>
     * Obtiene los idiomas en lso que está traducida la categoría con
     * identificador id.
     * </p>
     */
    public java.util.List obtenerIdiomasFromIdCategoria(java.lang.Long id);

    /**
     * <p>
     *   Does the same thing as {@link #obtenerIdiomasFromIdCategoria(java.lang.Long)} with an
     *   additional argument called <code>queryString</code>.  This <code>queryString</code>
     *   argument allows you to override the query string defined in {@link #obtenerIdiomasFromIdCategoria(java.lang.Long)}.
     * </p>
     */
    public java.util.List obtenerIdiomasFromIdCategoria(String queryString, java.lang.Long id);

    /**
     * <p>
     *   Does the same thing as {@link #obtenerIdiomasFromIdCategoria(java.lang.Long)} with an
     *   additional flag called <code>transform</code>.  If this flag is set to <code>TRANSFORM_NONE</code> then
     *   finder results will <strong>NOT</strong> be transformed during retrieval.
     *   If this flag is any of the other constants defined here
     *   then finder results <strong>WILL BE</strong> passed through an operation which can optionally
     *   transform the entities (into value objects for example).  By default, transformation does
     *   not occur.
     * </p>
     */
    public java.util.List obtenerIdiomasFromIdCategoria(int transform, java.lang.Long id);

    /**
     * <p>
     *   Does the same thing as {@link #obtenerIdiomasFromIdCategoria(boolean, java.lang.Long)} with an
     *   additional argument called <code>queryString</code>.  This <code>queryString</code>
     *   argument allows you to override the query string defined in {@link #obtenerIdiomasFromIdCategoria(int, java.lang.Long id)}.
     * </p>
     */
    public java.util.List obtenerIdiomasFromIdCategoria(int transform, String queryString, java.lang.Long id);

}
