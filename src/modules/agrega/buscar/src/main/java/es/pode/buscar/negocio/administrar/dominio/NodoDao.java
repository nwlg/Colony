// license-header java merge-point
//
// Attention: Generated code! Do not modify by hand!
// Generated by: SpringDao.vsl in andromda-spring-cartridge.
//
package es.pode.buscar.negocio.administrar.dominio;

/**
 * @see es.pode.buscar.negocio.administrar.dominio.Nodo
 */
public interface NodoDao
{
    /**
     * This constant is used as a transformation flag; entities can be converted automatically into value objects
     * or other types, different methods in a class implementing this interface support this feature: look for
     * an <code>int</code> parameter called <code>transform</code>.
     * <p/>
     * This specific flag denotes no transformation will occur.
     */
    public final static int TRANSFORM_NONE = 0;

    /**
     * This constant is used as a transformation flag; entities can be converted automatically into value objects
     * or other types, different methods in a class implementing this interface support this feature: look for
     * an <code>int</code> parameter called <code>transform</code>.
     * <p/>
     * This specific flag denotes entities must be transformed into objects of type
     * {@link es.pode.buscar.negocio.administrar.servicio.NodoVO}.
     */
    public final static int TRANSFORM_NODOVO = 1;

    /**
     * Converts this DAO's entity to an object of type {@link es.pode.buscar.negocio.administrar.servicio.NodoVO}.
     */
    public es.pode.buscar.negocio.administrar.servicio.NodoVO toNodoVO(es.pode.buscar.negocio.administrar.dominio.Nodo entity);

    /**
     * Converts this DAO's entity to a Collection of instances of type {@link es.pode.buscar.negocio.administrar.servicio.NodoVO}.
     */
    public void toNodoVOCollection(java.util.Collection entities);

    public es.pode.buscar.negocio.administrar.dominio.Nodo fromNodoVO(es.pode.buscar.negocio.administrar.servicio.NodoVO vo);
    
    public void fromNodoVO(es.pode.buscar.negocio.administrar.servicio.NodoVO vo, es.pode.buscar.negocio.administrar.dominio.Nodo entity);    


    /**
     * Loads an instance of es.pode.buscar.negocio.administrar.dominio.Nodo from the persistent store.
     */
    public es.pode.buscar.negocio.administrar.dominio.Nodo load(java.lang.Long id);

    /**
     * <p>
     *   Does the same thing as {@link #load(java.lang.Long)} with an
     *   additional flag called <code>transform</code>.  If this flag is set to <code>TRANSFORM_NONE</code> then
     *   the returned entity will <strong>NOT</strong> be transformed.  If this flag is any of the other constants
     *   defined in this class then the result <strong>WILL BE</strong> passed through an operation which can
     *   optionally transform the entity (into a value object for example).  By default, transformation does
     *   not occur.
     * </p>
     *
     * @param id the identifier of the entity to load.
     * @return either the entity or the object transformed from the entity.
     */
    public Object load(int transform, java.lang.Long id);
    
    /**
     * Loads all entities of type {@link es.pode.buscar.negocio.administrar.dominio.Nodo}. 
     * 
     * @return the loaded entities.
     */
    public java.util.Collection loadAll();
    
    /** 
     * <p>
     *   Does the same thing as {@link #loadAll()} with an
     *   additional flag called <code>transform</code>.  If this flag is set to <code>TRANSFORM_NONE</code> then
     *   the returned entity will <strong>NOT</strong> be transformed.  If this flag is any of the other constants
     *   defined here then the result <strong>WILL BE</strong> passed through an operation which can optionally
     *   transform the entity (into a value object for example).  By default, transformation does
     *   not occur.
     * </p>
     * 
     * @param transform the flag indicating what transformation to use.
     * @return the loaded entities.
     */
    public java.util.Collection loadAll(final int transform);

    /**
     * Creates an instance of es.pode.buscar.negocio.administrar.dominio.Nodo and adds it to the persistent store.
     */
    public es.pode.buscar.negocio.administrar.dominio.Nodo create(es.pode.buscar.negocio.administrar.dominio.Nodo nodo);

    /**
     * <p>
     *   Does the same thing as {@link #create(es.pode.buscar.negocio.administrar.dominio.Nodo)} with an
     *   additional flag called <code>transform</code>.  If this flag is set to <code>TRANSFORM_NONE</code> then
     *   the returned entity will <strong>NOT</strong> be transformed.  If this flag is any of the other constants
     *   defined here then the result <strong>WILL BE</strong> passed through an operation which can optionally
     *   transform the entity (into a value object for example).  By default, transformation does
     *   not occur.
     * </p>
     */
    public Object create(int transform, es.pode.buscar.negocio.administrar.dominio.Nodo nodo);

    /**
     * Creates a new instance of es.pode.buscar.negocio.administrar.dominio.Nodo and adds
     * from the passed in <code>entities</code> collection
     *
     * @param entities the collection of es.pode.buscar.negocio.administrar.dominio.Nodo
     *        instances to create.
     *
     * @return the created instances.
     */
    public java.util.Collection create(java.util.Collection entities);

    /**
     * <p>
     *   Does the same thing as {@link #create(es.pode.buscar.negocio.administrar.dominio.Nodo)} with an
     *   additional flag called <code>transform</code>.  If this flag is set to <code>TRANSFORM_NONE</code> then
     *   the returned entity will <strong>NOT</strong> be transformed.  If this flag is any of the other constants
     *   defined here then the result <strong>WILL BE</strong> passed through an operation which can optionally
     *   transform the entities (into value objects for example).  By default, transformation does
     *   not occur.
     * </p>
     */
    public java.util.Collection create(int transform, java.util.Collection entities);

    /**
     * <p>
     *   Creates a new <code>es.pode.buscar.negocio.administrar.dominio.Nodo</code>
     *   instance from <strong>all</strong> attributes and adds it to
     *   the persistent store.
     * </p>
     */
    public es.pode.buscar.negocio.administrar.dominio.Nodo create(
        java.lang.String nodo,
        java.lang.String url,
        java.util.Date fechaAlta,
        java.lang.String puerto,
        java.lang.String urlWS,
        java.lang.String idNodo);

    /**
     * <p>
     *   Does the same thing as {@link #create(java.lang.String, java.lang.String, java.util.Date, java.lang.String, java.lang.String, java.lang.String)} with an
     *   additional flag called <code>transform</code>.  If this flag is set to <code>TRANSFORM_NONE</code> then
     *   the returned entity will <strong>NOT</strong> be transformed.  If this flag is any of the other constants
     *   defined here then the result <strong>WILL BE</strong> passed through an operation which can optionally
     *   transform the entity (into a value object for example).  By default, transformation does
     *   not occur.
     * </p>
     */
    public Object create(
        int transform,
        java.lang.String nodo,
        java.lang.String url,
        java.util.Date fechaAlta,
        java.lang.String puerto,
        java.lang.String urlWS,
        java.lang.String idNodo);


    /**
     * Updates the <code>nodo</code> instance in the persistent store.
     */
    public void update(es.pode.buscar.negocio.administrar.dominio.Nodo nodo);

    /**
     * Updates all instances in the <code>entities</code> collection in the persistent store.
     */
    public void update(java.util.Collection entities);

    /**
     * Removes the instance of es.pode.buscar.negocio.administrar.dominio.Nodo from the persistent store.
     */
    public void remove(es.pode.buscar.negocio.administrar.dominio.Nodo nodo);

    /**
     * Removes the instance of es.pode.buscar.negocio.administrar.dominio.Nodo having the given
     * <code>identifier</code> from the persistent store.
     */
    public void remove(java.lang.Long id);

    /**
     * Removes all entities in the given <code>entities<code> collection.
     */
    public void remove(java.util.Collection entities);

    /**
     * <p>
     * Listado de los nodos existentes
     * </p>
     */
    public java.util.List listarNodos();

    /**
     * <p>
     *   Does the same thing as {@link #listarNodos()} with an
     *   additional argument called <code>queryString</code>.  This <code>queryString</code>
     *   argument allows you to override the query string defined in {@link #listarNodos()}.
     * </p>
     */
    public java.util.List listarNodos(String queryString);

    /**
     * <p>
     *   Does the same thing as {@link #listarNodos()} with an
     *   additional flag called <code>transform</code>.  If this flag is set to <code>TRANSFORM_NONE</code> then
     *   finder results will <strong>NOT</strong> be transformed during retrieval.
     *   If this flag is any of the other constants defined here
     *   then finder results <strong>WILL BE</strong> passed through an operation which can optionally
     *   transform the entities (into value objects for example).  By default, transformation does
     *   not occur.
     * </p>
     */
    public java.util.List listarNodos(int transform);

    /**
     * <p>
     *   Does the same thing as {@link #listarNodos(boolean)} with an
     *   additional argument called <code>queryString</code>.  This <code>queryString</code>
     *   argument allows you to override the query string defined in {@link #listarNodos(int)}.
     * </p>
     */
    public java.util.List listarNodos(int transform, String queryString);

    /**
     * 
     */
    public java.util.List ListarNodosSemejantes(es.pode.buscar.negocio.administrar.servicio.NodosCriteria criterios);

    /**
     * <p>
     *   Does the same thing as {@link #ListarNodosSemejantes(es.pode.buscar.negocio.administrar.servicio.NodosCriteria)} with an
     *   additional flag called <code>transform</code>.  If this flag is set to <code>TRANSFORM_NONE</code> then
     *   finder results will <strong>NOT</strong> be transformed during retrieval.
     *   If this flag is any of the other constants defined here
     *   then finder results <strong>WILL BE</strong> passed through an operation which can optionally
     *   transform the entities (into value objects for example).  By default, transformation does
     *   not occur.
     * </p>
     */
    public java.util.List ListarNodosSemejantes(final int transform, final es.pode.buscar.negocio.administrar.servicio.NodosCriteria criterios);

    /**
     * 
     */
    public java.util.List ListarNodosNombresSemejantes(es.pode.buscar.negocio.administrar.servicio.NombresNodosCriteria criterios);

    /**
     * <p>
     *   Does the same thing as {@link #ListarNodosNombresSemejantes(es.pode.buscar.negocio.administrar.servicio.NombresNodosCriteria)} with an
     *   additional flag called <code>transform</code>.  If this flag is set to <code>TRANSFORM_NONE</code> then
     *   finder results will <strong>NOT</strong> be transformed during retrieval.
     *   If this flag is any of the other constants defined here
     *   then finder results <strong>WILL BE</strong> passed through an operation which can optionally
     *   transform the entities (into value objects for example).  By default, transformation does
     *   not occur.
     * </p>
     */
    public java.util.List ListarNodosNombresSemejantes(final int transform, final es.pode.buscar.negocio.administrar.servicio.NombresNodosCriteria criterios);

}
